services:
  portfolio:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./public/photos:/app/public/photos
      - ./backend/photos.json:/app/backend/photos.json
      - ./cache:/app/cache            
    restart: unless-stopped